# Context Processor


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## imports

## registry-aware document loader

## URI resolution

- Step ① Try to dereference the URI (GET → follow redirects → 10 s
  timeout).
  - If the response is application/ld+json → return as-is.
  - If it’s Turtle (or any RDF serialisation rdflib can parse) → derive
    a minimal prefix context.
  - If it’s HTML but the Link: header advertises an application/ld+json
    alternate (e.g. Schema.org) → follow that link.
- Step ② If dereference fails (network error or unsupported format)
  fallback to the registry lookup (registry.resolve(url)).
- Step ③ Still nothing? raise JsonLdError with a LLM-friendly message
  and the chain of attempts in error.details.

Everything is fully cached with the same namespace-scoped memoizer you
already use, so repeated calls never hit the network twice.

## dereference helper

------------------------------------------------------------------------

<a
href="https://github.com/la3d/cogitarelink/blob/main/cogitarelink/core/context.py#L85"
target="_blank" style="float:right; font-size:smaller">source</a>

### get_document_loader

>  get_document_loader (network_first:bool=True)

\*Return a pyld‐compatible document loader.

- `network_first=True` – follow‐your-nose, registry is fallback.
- `network_first=False` – current behaviour (registry first, then
  HTTP).\*

## ContextProcessor

------------------------------------------------------------------------

<a
href="https://github.com/la3d/cogitarelink/blob/main/cogitarelink/core/context.py#L138"
target="_blank" style="float:right; font-size:smaller">source</a>

### ContextProcessor

>  ContextProcessor ()

*Thin wrapper around pyld with sensible defaults.*

## quick tests

## protected-term validator

------------------------------------------------------------------------

<a
href="https://github.com/la3d/cogitarelink/blob/main/cogitarelink/core/context.py#L222"
target="_blank" style="float:right; font-size:smaller">source</a>

### validate_context

>  validate_context (ctx:Dict[str,Any])

\*Validate a JSON-LD 1.1 context for: - Protected term redefinition
(§4.4.5) - Type-scoped context validity - @nest directives

Raises ValueError if validation fails.\*

``` python
validate_context(composer.compose(["vc","epcis"]))
```

------------------------------------------------------------------------

<a
href="https://github.com/la3d/cogitarelink/blob/main/cogitarelink/core/context.py#L269"
target="_blank" style="float:right; font-size:smaller">source</a>

### protected_terms

>  protected_terms (ctx)

*Extract protected terms from a JSON-LD 1.1 context*

------------------------------------------------------------------------

<a
href="https://github.com/la3d/cogitarelink/blob/main/cogitarelink/core/context.py#L299"
target="_blank" style="float:right; font-size:smaller">source</a>

### process_nest

>  process_nest (ctx, data)

*Process and apply @nest directives in a context to the data*
